name: Jerry CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      gate:
        description: Gate to run
        required: true
        default: ci-local
        type: choice
        options:
          - ci-local
          - release-gate-local

permissions:
  contents: read

jobs:
  ci:
    runs-on: [self-hosted, Windows, X64, jerry-ci, jerry-ci-win, p0, agent]
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Initialize toolchain PATH
        shell: pwsh
        run: |
          $candidatePaths = @(
            "$env:ProgramFiles\Git\cmd",
            "$env:USERPROFILE\scoop\apps\git\current\cmd",
            "$env:USERPROFILE\scoop\shims",
            "C:\Users\jerry\scoop\apps\git\current\cmd",
            "C:\Users\jerry\scoop\shims",
            "$env:ProgramFiles\nodejs",
            "$env:ProgramFiles\Docker\Docker\resources\bin",
            "$env:ProgramFiles\PowerShell\7"
          )
          $existingPaths = $candidatePaths | Where-Object { Test-Path -LiteralPath $_ }
          foreach ($path in $existingPaths) {
            Add-Content -LiteralPath $env:GITHUB_PATH -Value $path
          }
          $env:Path = (($existingPaths + ($env:Path -split ";" | Where-Object { $_ })) -join ";")

      - name: Show toolchain
        shell: pwsh
        run: |
          git --version
          node --version
          npm --version
          pwsh --version

      - name: Install dependencies
        shell: pwsh
        run: npm ci

      - name: Run selected gate
        shell: pwsh
        run: |
          $gate = "${{ github.event.inputs.gate }}"
          if ([string]::IsNullOrWhiteSpace($gate)) {
            $gate = "ci-local"
          }

          if ($gate -eq "ci-local") {
            npm run ci:local
          } elseif ($gate -eq "release-gate-local") {
            npm run release:gate:local
          } else {
            throw "Unknown gate: $gate"
          }
